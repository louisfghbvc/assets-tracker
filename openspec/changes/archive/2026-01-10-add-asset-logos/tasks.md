# Tasks: Add Asset Logos to List Items

- [x] Create `AssetLogo` sub-component in `App.tsx` (or a separate file).
- [x] Implement URL generation logic for TW Stocks, US Stocks, and Crypto.
- [x] Update the `asset-item` render block to use `AssetLogo`.
- [x] Add CSS for the new logo images (sizing, border-radius, background).
- [x] Verify fallbacks for non-existent logos.
- [x] Check if the logos correctly display in the `stats-card` and main list.
