# Tasks: Auto-Refresh on Application Startup

- [x] Add `hasInitialRefreshed` state to `App.tsx`.
- [x] Implement `useEffect` to trigger `handleRefresh` when `accessToken` is set.
- [x] Verify that the refresh only happens once per page load.
- [x] Ensure the sync status toast appears correctly during the automatic refresh.
