# Tasks: Fix Profit Display Layout Issues

- [x] Update `App.css` to stack `.stat-card-header` content on smaller screens.
- [x] Implement a `compactDisplayValue` helper in `App.tsx` or handle formatting directly.
- [x] Refine `.stat-card-pct` styles (padding, font-size) for mobile.
- [x] Verify layout on screens down to 320px.
- [x] Ensure `asset-item` badges don't overlap with names on ultra-thin screens.
