# Google OAuth Configuration
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# Cloudflare Worker CORS Proxy (Optional)
# If not set, will fall back to free public proxies (less reliable)
# Deploy your own worker: see workers/cors-proxy/README.md
VITE_CORS_PROXY_URL=https://assettracker-cors-proxy.YOUR_SUBDOMAIN.workers.dev/proxy

# Note: Without the worker proxy, price fetching may have ~85-95% success rate
# With the worker proxy, expect 100% success rate
